import { GalleryItem, ImageComparison } from '@/types';

// Add confirmed gameplay screenshots here as they become part of the Seven Lakes record.
export const galleryItems: GalleryItem[] = [];

// Selected regional transformations. Add entries only when both real images exist.
export const galleryComparisons: ImageComparison[] = [];
